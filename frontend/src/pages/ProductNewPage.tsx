import { Container, Typography } from "@mui/material";

export default function ProductNewPage() {
  return (
    <Container sx={{ py: 4 }}>
      <Typography variant="h5">New Product</Typography>
      <Typography sx={{ mt: 2 }}>Formulaire à venir.</Typography>
    </Container>
  );
}
